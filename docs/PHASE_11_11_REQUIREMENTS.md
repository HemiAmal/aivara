# Phase 11.11 — Comprehensive Distribution Shift Verification
## Formal Verification Requirements Specification

**Document ID:** `DOC-11-11-REQ-SPEC`  
**Phase:** Phase 11.11 (Verification Planning & Architecture Only)  
**Total Formal Requirements:** 85 Traceable Verification Requirements  
**Status:** Permanent Freeze Baseline  

---

### Layer 1: Unit Correctness Requirements (REQ-11-VERIF-001 to REQ-11-VERIF-010)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-001** | Kolmogorov-Smirnov 2-sample test validity | Phase 11.3 | Returns valid KS statistic $D \in [0, 1]$ and p-value $p \in [0, 1]$. Invariant: identical distributions yield $D \to 0, p \to 1$. | Unit test with known Gaussian distributions and edge case sample sets. | `StatisticalTestResult` payload. | $D = 0.0, p = 1.0$ for identical inputs; $D > 0.5, p < 10^{-4}$ for separated distributions. | `BLOCKER` |
| **REQ-11-VERIF-002** | Wasserstein-1 Distance metric accuracy | Phase 11.3 | Computes $L_1$ earth mover distance between empirical 1D distributions. Zero for identical distributions. | Unit test against analytical shifted distributions ($X \sim \mathcal{N}(0, 1), Y \sim \mathcal{N}(\mu, 1)$). | `DriftMetricResult` object. | Measured $W_1$ matches $|\mu|$ within $\pm 0.05$ sampling tolerance. | `CRITICAL` |
| **REQ-11-VERIF-003** | Population Stability Index (PSI) calculation | Phase 11.3 | Reference-derived quantile binning with Laplace $\epsilon$-smoothing ($10^{-4}$) to prevent $\log(0)$ division by zero. | Unit test with zero-count target bins and identical distributions. | PSI numerical output. | $\text{PSI} \approx 0.0$ for identical; $\text{PSI} \ge 0.25$ for severe shift; zero `NaN`/`Inf` occurrences. | `CRITICAL` |
| **REQ-11-VERIF-004** | Categorical Chi-Square & TVD computation | Phase 11.3 | Evaluates $\chi^2$ goodness-of-fit and Total Variation Distance ($\text{TVD} = \frac{1}{2}\sum \|p_i - q_i\|$). | Unit test with matched, shifted, and disjoint categorical frequency vectors. | Categorical drift test result. | $\text{TVD} \in [0, 1]$; $\chi^2$ p-value $\le 0.05$ when category proportions diverge significantly. | `CRITICAL` |
| **REQ-11-VERIF-005** | Jensen-Shannon Divergence symmetry and boundedness | Phase 11.3 | Computes $\text{JSD}(P \| Q) \in [0, 1]$ using base-2 logarithm. Invariant: $\text{JSD}(P \| Q) = \text{JSD}(Q \| P)$. | Unit test testing symmetry and extreme disjoint distributions. | JSD numerical output. | $\text{JSD}(P \| Q) == \text{JSD}(Q \| P)$; $\text{JSD} = 1.0$ for completely disjoint supports. | `MAJOR` |
| **REQ-11-VERIF-006** | Kernel Maximum Mean Discrepancy (MMD) | Phase 11.3 | Computes unbiased quadratic-time MMD with RBF kernel median heuristic bandwidth and permutation testing ($B=100$). | Unit test with multivariate Gaussian point clouds. | Multivariate test result. | $\text{MMD}^2 \approx 0, p > 0.05$ for identical multivariate clouds; $\text{MMD}^2 > 0.05, p \le 0.01$ for shifted clouds. | `CRITICAL` |
| **REQ-11-VERIF-007** | Energy Distance computation | Phase 11.3 | Nonparametric multivariate energy distance between continuous feature matrices. Invariant: $E(X, Y) \ge 0$. | Unit test against identical and perturbed vector matrices. | Energy distance output. | $E(X, X) == 0.0$; $E(X, Y) > 0.0$ for $X \ne Y$. | `MAJOR` |
| **REQ-11-VERIF-008** | Benjamini-Hochberg FDR multiplicity adjustment | Phase 11.3 | Controls False Discovery Rate at $q^* = 0.05$ across $M$ hypothesis tests. Strictly enforces $p_{\text{adj}} \le 1.0$ and monotonicity. | Unit test with array of synthetic p-values ($[0.001, 0.01, 0.04, 0.20, 0.80]$). | Adjusted p-value array. | $p_{\text{adj}}$ values match step-up formula $\min_{j \ge i} (p_j \cdot M / j)$; monotonic non-decreasing. | `CRITICAL` |
| **REQ-11-VERIF-009** | Minimum sample size floor enforcement | Phase 11.2 | Rejects datasets with $N < N_{\min} = 30$ with `InsufficientDataError`. | Negative unit test with $N=10, 20, 29$. | Exception log. | Throws `InsufficientDataError`; zero partial calculation executed. | `CRITICAL` |
| **REQ-11-VERIF-010** | Maximum sample size ceiling & deterministic subsampling | Phase 11.2 | Subsamples populations with $N > N_{\max} = 5000$ to exactly 5000 using seed 42. | Unit test with $N=10,000$ dataset. | `PopulationSet` metadata. | Resulting sample size exactly 5000; deterministic sample ID set across repeated calls. | `MAJOR` |

---

### Layer 2: Component Integration Requirements (REQ-11-VERIF-011 to REQ-11-VERIF-020)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-011** | Feature dataset drift engine statistical delegation | Phase 11.4 | `FeatureDatasetDriftAnalyzer` delegates all univariate/categorical tests directly to Phase 11.3 `StatisticalDriftEngine`. | Code inspection + mock assertion during execution. | Execution call trace. | 100% of two-sample tests routed to `StatisticalDriftEngine`; 0 duplicate statistical functions invoked. | `BLOCKER` |
| **REQ-11-VERIF-012** | Image drift analyzer descriptor extraction integration | Phase 11.5 | `ImageDistributionShiftAnalyzer` extracts Dimension, Pixel, and Quality descriptor families and delegates comparisons to Phase 11.3. | Multi-image batch execution (100 synthetic JPEG/PNG images). | `ImageDriftProfile` object. | Descriptors extracted for all analyzable images; statistical comparisons executed via Phase 11.3 engine. | `CRITICAL` |
| **REQ-11-VERIF-013** | Representation drift analyzer ONNX pipeline | Phase 11.6 | Ingests reference/target feature matrices, verifies dimension $D \le 4096$, normalizes L2 embeddings ($\Vert v \Vert_2 = 1$), and executes MMD/Energy tests. | Representation engine execution with synthetic embedding arrays. | `RepresentationDriftProfile`. | All vectors normalized ($\Vert v_i \Vert_2 = 1.0 \pm 10^{-6}$); MMD and Energy distance results populated. | `CRITICAL` |
| **REQ-11-VERIF-014** | Temporal analyzer window partitioning & dual topology | Phase 11.7 | Partitions time-ordered stream into $K \le 50$ windows, computes Baseline ($\mathcal{W}_0 \leftrightarrow \mathcal{W}_k$) and Adjacent ($\mathcal{W}_{k-1} \leftrightarrow \mathcal{W}_k$) metrics. | Temporal analyzer execution with time-series dataset of 1,000 samples across 10 days. | `TemporalTrajectoryReport`. | Exactly $K$ baseline comparisons and $K-1$ adjacent comparisons produced; correct window boundaries. | `CRITICAL` |
| **REQ-11-VERIF-015** | Temporal trajectory classification state machine | Phase 11.7 | Correctly classifies trajectory into `NO_MATERIAL_SHIFT`, `TRANSIENT_SHIFT`, `PERSISTENT_SHIFT`, `GRADUAL_DRIFT`, or `ABRUPT_SHIFT`. | Synthetic time series representing step jump, drift, transient spike, and stationary data. | State enum field in profile. | Trajectory state matches ground truth simulation for each synthetic scenario. | `CRITICAL` |
| **REQ-11-VERIF-016** | Contributor source engine 5-stage canonicalization | Phase 11.8 | Standardizes source identifiers: NFKC $\to$ strip non-printables $\to$ collapse whitespace $\to$ lowercase $\to$ cap 128 chars. | Unit/integration test with dirty, accented, whitespace-padded contributor names. | Canonical source identifier strings. | Equivalent strings (e.g., `"  User-Alpha \t"`, `"user-alpha"`, `"USER-ALPHA"`) yield identical canonical IDs. | `CRITICAL` |
| **REQ-11-VERIF-017** | Contributor project-scoped pseudonymization | Phase 11.8 | Generates project-scoped pseudonym `SHA-256(project_id \|\| salt \|\| canonical_id)[:16]`. Invariant: cross-project unlinkability. | Execute across two distinct project IDs with identical contributor IDs. | Emitted pseudonym strings. | Pseudonyms for same contributor differ between Project A and Project B; identical within same project. | `CRITICAL` |
| **REQ-11-VERIF-018** | Contributor source engine $O(G)$ linear topology | Phase 11.8 | Evaluates each source group against certified reference ($\mathcal{P}_{s_g} \leftrightarrow \mathcal{P}_{\text{ref}}$). Prohibits $O(G^2)$ all-pairs comparisons. | Execution with $G=10$ source groups. | Comparison count log. | Exactly 10 comparisons performed (1 per group vs reference); zero inter-group comparisons executed. | `MAJOR` |
| **REQ-11-VERIF-019** | Simpson's paradox & label confounding safeguard | Phase 11.8 | Emits label confounding advisory finding when class distribution $\text{TVD} \ge 0.15$ alongside feature shift. | Integration test with class-specialized contributor (contributes 95% Class 1). | `FindingModel` with `finding_type="label_confounding_advisory"`. | Confounding advisory generated; prevented naive misattribution of feature shift. | `CRITICAL` |
| **REQ-11-VERIF-020** | Multi-modal risk engine ancestry clustering | Phase 11.9 | Clusters findings sharing same underlying asset lineage into unified modality clusters before risk aggregation. | Provide 5 findings (3 feature drift, 2 image drift) referencing same dataset version. | `RiskAssessmentModel.modality_clusters`. | Findings correctly grouped into 2 modality clusters (feature, image); no naive linear sum of confidences. | `BLOCKER` |

---

### Layer 3: Cross-Component Consistency Requirements (REQ-11-VERIF-021 to REQ-11-VERIF-027)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-021** | Cross-engine population count reconciliation | Phases 11.2–11.8 | Reference and target sample counts ($N_{\text{ref}}, N_{\text{tgt}}$) reported across Feature, Image, Representation, Temporal, and Source engines are identical. | Multi-modal pipeline execution on shared synthetic dataset. | Profile summary tables. | $N_{\text{ref}}$ and $N_{\text{tgt}}$ are identical across all generated profiles. | `CRITICAL` |
| **REQ-11-VERIF-022** | Population contract digest immutability across engines | Phases 11.2–11.8 | `population_contract_hash` generated in Phase 11.2 is preserved unchanged across all child profile outputs. | Inspect all emitted profiles from a single pipeline run. | `population_contract_hash` fields. | Bit-for-bit identical 64-character SHA-256 hex string in all profile headers. | `BLOCKER` |
| **REQ-11-VERIF-023** | Schema definition symmetry across engines | Phases 11.2–11.4 | Feature schema, column names, and data type enums are identically reflected between Boundary and Feature engines. | Cross-check `PopulationContract.feature_schema` and `FeatureDriftProfile.feature_profiles`. | Schema key sets. | Exact set equality: $\text{keys}(\text{contract}) == \text{keys}(\text{feature\_profiles})$. | `CRITICAL` |
| **REQ-11-VERIF-024** | Consistent dual-gating threshold application | Phases 11.4–11.8 | Every engine uses $p_{\text{adj}} \le 0.05$ as statistical significance floor and exact domain effect thresholds. | Inspect decision criteria across feature, image, representation, temporal, source engines. | Drift flag boolean values. | `drift_detected == True` if and only if $p_{\text{adj}} \le 0.05$ AND $\text{effect} \ge \tau$. | `CRITICAL` |
| **REQ-11-VERIF-025** | Descriptive finding type taxonomy consistency | Phases 11.4–11.9 | Emitted findings strictly conform to the standardized finding type enum hierarchy. | Enumerate all finding objects emitted across multi-modal run. | `FindingModel.finding_type`. | All types belong to frozen enum (`feature_drift`, `image_drift`, `representation_drift`, `temporal_drift`, `source_drift`). | `MAJOR` |
| **REQ-11-VERIF-026** | Consistent evidence layer categorization | Phases 11.4–11.9 | All distribution shift findings are stamped `evidence_layer="detection"`; proof integrity findings stamped `evidence_layer="proof"`. | Inspect evidence layer tags across all generated findings. | `EvidenceModel.evidence_layer`. | 100% of drift findings have `evidence_layer == "detection"`; zero mislabeled as "proof". | `BLOCKER` |
| **REQ-11-VERIF-027** | Zero duplicate statistical engine instances | Phases 11.2–11.10 | Codebase inspection confirms zero alternative statistical hypothesis testing implementations. | AST / Grep codebase scan for `scipy.stats`, `ks_2samp`, `chi2_contingency` outside `drift/engine.py` and `drift/stats_*.py`. | AST scan output. | Zero external statistical calls outside authoritative Phase 11.3 modules. | `BLOCKER` |

---

### Layer 4: End-to-End Assurance Integration Requirements (REQ-11-VERIF-028 to REQ-11-VERIF-037)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-028** | Multi-modal correlation damping application | Phase 11.9 | Inter-modality correlation damping ($\lambda_{\text{corr}} = 0.10$) is applied to secondary findings within the same modality cluster. | Execute risk synthesis with 3 findings of confidence $[0.8, 0.6, 0.4]$ in same cluster. | Cluster score calculation. | Effective cluster score $S(\mathcal{C}) = 0.8 + 0.10 \times (0.6 + 0.4) = 0.90 \le 1.0$. | `BLOCKER` |
| **REQ-11-VERIF-029** | Bounded asymptotic risk score computation | Phase 11.9 | Multi-cluster risk aggregation $R = 1 - \prod_{k=1}^K (1 - S(\mathcal{C}_k))$ produces $R \in [0.0, 1.0]$. | Test with 1, 5, 10 independent clusters with various cluster scores. | `RiskAssessmentModel.overall_risk_score`. | For all inputs, $0.0 \le R \le 1.0$; $R$ monotonically increases with each additional non-zero cluster. | `BLOCKER` |
| **REQ-11-VERIF-030** | Deterministic disposition policy threshold mapping | Phase 11.9 | Maps risk score to: $R < 0.30 \to \text{ACCEPT}$, $[0.30, 0.65) \to \text{REVIEW}$, $[0.65, 0.85) \to \text{QUARANTINE}$, $R \ge 0.85 \to \text{REJECT}$. | Synthesize risk scores across boundary values ($0.0, 0.299, 0.300, 0.649, 0.650, 0.849, 0.850, 1.0$). | `RiskAssessmentModel.disposition`. | Exact enum match for every boundary test case. | `CRITICAL` |
| **REQ-11-VERIF-031** | Non-compensable cryptographic proof override | Phase 11.9 | If any Proof Layer violation exists, disposition is forced to $\mathbf{REJECT}$ ($R = 1.0$) regardless of detection scores. | Inject a Proof Layer failure finding alongside low detection drift scores ($R_{\text{det}} = 0.05$). | `RiskAssessmentModel.disposition`. | Disposition is $\mathbf{REJECT}$; rationale documents non-compensable proof violation override. | `BLOCKER` |
| **REQ-11-VERIF-032** | Fail-closed handling of insufficient evidence | Phase 11.9 | If required modality data is missing or unanalyzable, disposition is $\mathbf{INSUFFICIENT\_EVIDENCE}$. | Execute analysis with missing target population data. | `RiskAssessmentModel.disposition`. | Disposition is $\mathbf{INSUFFICIENT\_EVIDENCE}$; zero false $\text{ACCEPT}$ emitted. | `CRITICAL` |
| **REQ-11-VERIF-033** | Integrated assurance profile cryptographic digest | Phase 11.9 | Computes RFC 8785 JCS SHA-256 `integrated_profile_hash` over synthesized profile. | Generate profile, mutate any single finding field, recompute digest. | `integrated_profile_hash`. | Original digest matches RFC 8785 reference; mutated profile produces completely distinct SHA-256 digest. | `BLOCKER` |
| **REQ-11-VERIF-034** | Evidence set immutability & cryptographic grounding | Phase 11.9 | `evidence_set_hash` computed over canonical sort of evidence hashes. | Inspect evidence set hashing in assurance engine. | `evidence_set_hash`. | Reordering input evidence items produces identical `evidence_set_hash`. | `CRITICAL` |
| **REQ-11-VERIF-035** | Risk policy & decision policy digest attestation | Phase 11.9 | Policies carry `risk_policy_hash` and `decision_policy_hash` ensuring configuration tamper evidence. | Verify policy objects emitted in risk assessment. | Policy hash fields. | Policy hashes match SHA-256 of canonicalized policy configuration parameters. | `MAJOR` |
| **REQ-11-VERIF-036** | Strict non-attribution descriptive findings | Phase 11.9 | Verification that all synthesized findings, summaries, and rationales contain zero accusatory or malice keywords. | Text scan over all emitted findings against prohibited terminology blocklist (`"malicious"`, `"attacker"`, `"sabotage"`, `"guilty"`). | Rationale strings. | Zero occurrences of prohibited terms in finding descriptions and rationales. | `CRITICAL` |
| **REQ-11-VERIF-037** | Full lineage linkage from findings to evidence records | Phase 11.9 | Every emitted `FindingModel` contains valid foreign key references (`evidence_ids`) pointing to existing evidence items. | Graph traversal verification over emitted risk assessment payload. | Cross-reference list. | 100% of finding `evidence_ids` resolve to valid items in the `evidence` list. | `CRITICAL` |

---

### Layer 5: API & Task Orchestration Requirements (REQ-11-VERIF-038 to REQ-11-VERIF-048)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-038** | Asynchronous task creation (HTTP 202 Accepted) | Phase 11.10 | `POST /api/v1/projects/{project_id}/drift/analyses` returns HTTP 202 with `task_id` and initial status `QUEUED`. | API endpoint call with valid request payload. | HTTP response payload. | Status code 202; `task_id` valid UUID string; status == `QUEUED`. | `BLOCKER` |
| **REQ-11-VERIF-039** | Asynchronous execution via ThreadPoolExecutor | Phase 11.10 | Analysis executes asynchronously on background worker thread without blocking FastAPI event loop. | Dispatch long analysis, concurrently query health check endpoint `/health`. | Latency logs. | Health check responds in $< 10\text{ms}$ while drift task runs in background. | `CRITICAL` |
| **REQ-11-VERIF-040** | Deterministic 6-state machine transition sequence | Phase 11.10 | Task transitions: `QUEUED` $\to$ `RUNNING` $\to$ `COMPLETED` (or `FAILED` / `CANCELLED`). Illegal transitions rejected. | Monitor task status polling throughout execution. | Task status history. | Valid linear state progression; zero illegal transitions (e.g. `COMPLETED` $\to$ `RUNNING`). | `BLOCKER` |
| **REQ-11-VERIF-041** | Idempotency via RFC 8785 request fingerprinting | Phase 11.10 | Identical request with same `Idempotency-Key` returns existing task without duplicate execution. | Submit identical `POST` request twice with same `Idempotency-Key` header. | HTTP response headers and body. | Second request returns existing `task_id`; zero new background jobs spawned. | `CRITICAL` |
| **REQ-11-VERIF-042** | Idempotency conflict detection (HTTP 409) | Phase 11.10 | Submitting conflicting payload with previously used `Idempotency-Key` returns HTTP 409 Conflict. | Submit modified payload using same `Idempotency-Key`. | HTTP error response. | Status code 409; error code `IDEMPOTENCY_CONFLICT`. | `CRITICAL` |
| **REQ-11-VERIF-043** | Cooperative task cancellation | Phase 11.10 | `POST .../analyses/{task_id}/cancel` sets `CANCEL_REQUESTED` and cleanly transitions to `CANCELLED`. | Submit cancel request while task is `RUNNING`. | Final task status. | Task stops execution promptly; final status `CANCELLED`; zero partial DB corruption. | `CRITICAL` |
| **REQ-11-VERIF-044** | Server-Sent Events (SSE) stream delivery | Phase 11.10 | `GET .../analyses/{task_id}/events` streams real-time progress events with monotonic sequence numbers. | Connect SSE client to running task stream. | Received event stream. | Events received in strictly increasing sequence ($0, 1, 2, \dots$); final event is terminal state. | `CRITICAL` |
| **REQ-11-VERIF-045** | SSE reconnection replay via `Last-Event-ID` | Phase 11.10 | Connecting with `Last-Event-ID: 2` replays buffered events with sequence numbers $> 2$. | Disconnect SSE client at seq 2, reconnect specifying `Last-Event-ID: 2`. | Replayed event stream. | Server replays events starting at sequence 3 without missing or duplicating events. | `MAJOR` |
| **REQ-11-VERIF-046** | Result retrieval endpoint (HTTP 200) | Phase 11.10 | `GET .../analyses/{task_id}/result` returns complete `ApiResponse[DriftAnalysisResponse]` once completed. | Query result endpoint for completed task. | JSON response payload. | Status code 200; payload contains full profile, findings, risk assessment, and cryptographic digests. | `BLOCKER` |
| **REQ-11-VERIF-047** | Result retrieval on incomplete task (HTTP 409 / 404) | Phase 11.10 | Querying result endpoint while task is `QUEUED` or `RUNNING` returns appropriate status without partial leakage. | Query result endpoint before task finishes. | HTTP error response. | Returns HTTP 409 Conflict indicating task still in progress. | `MAJOR` |
| **REQ-11-VERIF-048** | Sanitized error responses & path suppression | Phase 11.10 | Error responses return standardized `ApiErrorResponse` with sanitized messages; zero internal stack traces or paths. | Trigger backend exception via invalid payload. | HTTP error JSON body. | No Python tracebacks, filesystem paths, or SQLite internal errors in response body. | `CRITICAL` |

---

### Layer 6: Security & Authorization Requirements (REQ-11-VERIF-049 to REQ-11-VERIF-055)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-049** | Broken Object Level Authorization (BOLA) project isolation | Phase 11.10 | Querying a task belonging to Project A using Project B's endpoint URL returns HTTP 404 Not Found. | Cross-tenant access test: Request task created in `proj_alpha` via `/api/v1/projects/proj_beta/drift/analyses/{task_id}`. | HTTP status and body. | Returns HTTP 404; zero task metadata or status disclosed. | `BLOCKER` |
| **REQ-11-VERIF-050** | Path traversal rejection in dataset & model paths | Phase 11.6, 11.10 | File paths containing `../`, `..\\`, absolute root paths, or null bytes are rejected with HTTP 400/422. | Submit payload with `model_path: "../../../etc/passwd"` or `dataset_path: "C:\\Windows\\System32"`. | API validation error. | Rejected at schema/service boundary; zero filesystem traversal. | `BLOCKER` |
| **REQ-11-VERIF-051** | Rejection of unauthenticated remote URLs | Phases 11.2–11.10 | Dataset paths or model paths starting with `http://`, `https://`, `ftp://`, or `s3://` are strictly rejected. | Submit payload with `reference_path: "https://evil.com/data.parquet"`. | Validation exception. | Immediate validation error; zero outbound socket creation attempted. | `BLOCKER` |
| **REQ-11-VERIF-052** | In-memory SSE subscriber connection limit | Phase 11.10 | Prevents memory exhaustion by limiting concurrent SSE subscribers per task to $\le 10$. | Spawn 15 concurrent SSE listener connections to a single task. | Connection status codes. | Excess connections rejected with HTTP 429 Too Many Requests; server memory stable. | `MAJOR` |
| **REQ-11-VERIF-053** | Denial of Service protection via payload size limits | Phase 11.10 | Ingesting oversized raw JSON payloads ($> 10\text{MB}$) is rejected by FastAPI middleware. | Post oversized payload ($15\text{MB}$) to analysis endpoint. | HTTP status code. | Returns HTTP 413 Payload Too Large; zero memory allocation spike. | `MAJOR` |
| **REQ-11-VERIF-054** | Contributor salt protection & non-reversibility | Phase 11.8 | Pseudonym salt is securely managed per project; raw contributor PII is never logged or returned in API responses. | Inspect logs, database records, and API JSON responses for raw contributor IDs. | Log & DB audit dump. | Raw contributor IDs absent; only 16-char pseudonyms present. | `CRITICAL` |
| **REQ-11-VERIF-055** | Safe deserialization without arbitrary code execution | Phases 11.2–11.10 | Ingestion of tabular, image, and metadata formats uses safe parsers (e.g. Parquet/Arrow, Pillow safe mode); `pickle.loads` prohibited. | AST scan of all Phase 11 codebase for `pickle`, `eval`, `exec`, `yaml.unsafe_load`. | AST scan output. | Zero unsafe deserialization calls detected. | `BLOCKER` |

---

### Layer 7: Determinism & Repeatability Requirements (REQ-11-VERIF-056 to REQ-11-VERIF-062)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-056** | End-to-end multi-modal pipeline repeatability | Phases 11.2–11.10 | Executing the complete analysis pipeline twice on identical datasets with seed 42 produces bit-for-bit identical outputs. | Run pipeline twice independently on same reference/target datasets. | Compare entire JSON output structures. | All profile hashes, finding confidences, risk scores, and disposition enums match 100%. | `BLOCKER` |
| **REQ-11-VERIF-057** | Deterministic permutation test seeding | Phase 11.3 | Permutation hypothesis tests ($B=100$) in MMD and continuous engines use deterministic PRNG seeds. | Run MMD permutation test 5 times with fixed seed. | Resulting p-value list. | All 5 computed p-values are identical to machine precision. | `CRITICAL` |
| **REQ-11-VERIF-058** | Deterministic UTC microsecond timestamp formatting | Phase 11.7 | All temporal timestamps formatted to ISO 8601 UTC strings (`YYYY-MM-DDTHH:MM:SS.ffffffZ`). | Test timestamp formatter across timezones (UTC, EST, JST). | Formatted string output. | All timezones converted to exact equivalent UTC timestamp string. | `MAJOR` |
| **REQ-11-VERIF-059** | Deterministic multi-key sorting order | Phase 11.7 | Temporal sample sorting enforces Primary key `timestamp ASC`, Secondary key `SHA-256(sample) ASC`. | Submit samples with identical timestamps in randomized order. | Sorted sample ID sequence. | Resulting sequence is 100% deterministic regardless of input array ordering. | `CRITICAL` |
| **REQ-11-VERIF-060** | Canonical key ordering in RFC 8785 serialization | Phases 11.2–11.10 | JSON serialization lexicographically sorts object keys according to RFC 8785 UTF-16 code unit order. | Serialize dictionaries with randomly ordered keys. | Serialized byte string. | Output byte sequence is identical for all key order permutations. | `BLOCKER` |
| **REQ-11-VERIF-061** | Invariant floating-point numerical formatting in digests | Phases 11.2–11.10 | Floating point values formatted consistently without platform-dependent trailing decimal variance. | Canonicalize float values ($0.0, 1.0, 0.123456789$). | Serialized JCS string. | Formatted floats strictly adhere to RFC 8785 float representation rules. | `CRITICAL` |
| **REQ-11-VERIF-062** | Thread-safety & isolation in concurrent task execution | Phase 11.10 | Running 4 concurrent drift analyses on separate threads produces identical outputs to serial runs. | Execute 4 identical analyses concurrently in thread pool vs. serially. | Output comparison table. | Concurrent results match serial results exactly; zero race condition artifacts. | `CRITICAL` |

---

### Layer 8: Resource Governance & Complexity Requirements (REQ-11-VERIF-063 to REQ-11-VERIF-067)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-063** | Maximum window limit enforcement ($K \le 50$) | Phase 11.7 | Temporal requests specifying $> 50$ windows are rejected with validation error. | Submit temporal configuration with $K = 100$. | Validation exception. | Throws `ValidationError`; window count capped at 50. | `MAJOR` |
| **REQ-11-VERIF-064** | Maximum source group limit enforcement ($G \le 50$) | Phase 11.8 | Ingesting dataset with $> 50$ distinct source groups processes top 50 by volume and flags remainder in accounting. | Submit dataset with 80 source groups. | `SourceDriftProfile.accounting`. | Exactly 50 groups evaluated; excess groups accounted for in `unsupported_groups_count`. | `MAJOR` |
| **REQ-11-VERIF-065** | Maximum feature dimensionality bound ($D \le 4096$) | Phase 11.2 | Feature or representation matrices with $D > 4096$ are rejected at population boundary. | Submit dataset with $D = 5000$ columns. | Boundary exception. | Throws `DimensionalityExceededError`; zero memory allocation for excess columns. | `CRITICAL` |
| **REQ-11-VERIF-066** | Linear computational complexity verification ($O(G)$) | Phase 11.8 | Execution time for contributor analysis scales linearly $O(G)$ with group count, not quadratic $O(G^2)$. | Measure execution time for $G=5, 10, 20, 40$ groups. | Timing benchmark curve. | Time scaling factor matches linear ratio $T(40) / T(10) \approx 4.0 \pm 0.5$ (not 16x). | `MAJOR` |
| **REQ-11-VERIF-067** | Memory ceiling boundedness ($< 100\text{MB}$ per task) | Phases 11.2–11.10 | Peak memory delta during end-to-end multi-modal analysis on $N=5000$ samples remains $< 100\text{MB}$. | Profile memory allocation using `tracemalloc` during full run. | Peak memory log. | Peak heap delta $< 100\text{MB}$; memory cleanly deallocated on task completion. | `MAJOR` |

---

### Layer 9: Cryptographic Integrity Requirements (REQ-11-VERIF-068 to REQ-11-VERIF-073)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-068** | Population contract hash SHA-256 integrity | Phase 11.2 | `contract_hash` matches `SHA-256(JCS(PopulationContract))`. | Compute expected digest via reference RFC 8785 implementation. | Computed vs reported digest. | 100% bit match (64 hex characters). | `BLOCKER` |
| **REQ-11-VERIF-069** | Population set sample content hash | Phase 11.2 | `population_hash` matches `SHA-256(sorted_sample_hashes)`. | Verify population set hashing. | Hash comparison. | Digest changes if any single sample ID or content byte is altered. | `BLOCKER` |
| **REQ-11-VERIF-070** | Analytical drift profile hash integrity | Phases 11.4–11.8 | Each domain profile (`FeatureDriftProfile`, `ImageDriftProfile`, etc.) computes valid `profile_hash`. | Mutate profile field; recompute digest. | Profile hash comparison. | Original matches standard; any modification alters digest. | `BLOCKER` |
| **REQ-11-VERIF-071** | Integrated assurance profile hash integrity | Phase 11.9 | `integrated_profile_hash` computed over canonical JCS representation of assurance profile. | Validate hash calculation on complete integrated profile. | Profile digest string. | Valid RFC 8785 SHA-256 hex string; sensitive to all child findings. | `BLOCKER` |
| **REQ-11-VERIF-072** | API request fingerprint deterministic hashing | Phase 11.10 | `request_fingerprint` matches `SHA-256(JCS(canonical_request_dict))`. | Compute fingerprint for identical requests with different whitespace/key orders. | Request fingerprint string. | Identical fingerprint generated across all semantically equivalent requests. | `CRITICAL` |
| **REQ-11-VERIF-073** | Operational ID vs. Cryptographic Hash separation | Phases 11.2–11.10 | System strictly distinguishes operational UUIDs (`task_id`, `analysis_id`) from cryptographic content digests (`profile_hash`). | Inspect code paths ensuring operational UUIDs are excluded from cryptographic digest payloads. | Digest serialization schemas. | Operational UUIDs not included in RFC 8785 hash payloads; hash depends solely on immutable content. | `CRITICAL` |

---

### Layer 10: 100% Offline Air-Gap Requirements (REQ-11-VERIF-074 to REQ-11-VERIF-077)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-074** | Zero outbound socket / network calls during analysis | Phases 11.2–11.10 | Analysis execution creates 0 network connections (HTTP, TCP, UDP, DNS). | Run analysis under socket mocking/blocking interceptor. | Network intercept log. | Zero socket creation or connection attempts recorded. | `BLOCKER` |
| **REQ-11-VERIF-075** | Zero external telemetry or analytics transmission | Phases 11.2–11.10 | Codebase contains zero telemetry SDKs (e.g. Sentry, Segment, Google Analytics, PostHog). | AST / import scan across all repository modules. | Module import list. | Zero telemetry packages imported or referenced. | `BLOCKER` |
| **REQ-11-VERIF-076** | Zero unauthenticated remote model downloading | Phase 11.6 | Representation engine requires pre-installed local ONNX models; never queries HuggingFace or remote repositories. | Execute representation analysis without internet connection. | Network / filesystem logs. | Operates purely from local filesystem path; throws local error if missing. | `BLOCKER` |
| **REQ-11-VERIF-077** | Local SQLite persistence air-gap compliance | Phases 11.2–11.10 | All results, tasks, evidence, and findings persist exclusively to local SQLite database file. | Inspect database connection URI and execution traces. | SQLite connection strings. | Connection URI points to local file (`sqlite:///...`); 0 external DB connections. | `BLOCKER` |

---

### Layer 11: Non-Regression Requirements (REQ-11-VERIF-078 to REQ-11-VERIF-081)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-078** | Complete historical test suite preservation | Phases 0–10, 11.1–11.10 | All 2,102 existing unit, integration, and regression tests continue to pass with 100% success. | Full pytest execution over `tests/`. | Pytest summary report. | 2,102 passed, 0 failed, 0 errors. | `BLOCKER` |
| **REQ-11-VERIF-079** | Zero database schema modifications or migrations | Phases 11.2–11.10 | Verification introduces 0 new Alembic migrations and 0 SQL table/column modifications. | Check `alembic/versions/` and SQLAlchemy model definitions. | Git status & schema diff. | 0 new migration files; 0 modified DB model files. | `BLOCKER` |
| **REQ-11-VERIF-080** | Zero new third-party dependencies | Phases 11.2–11.10 | Zero new Python packages added to `pyproject.toml`, `requirements.txt`, or environment. | Inspect package configuration files. | Dependency diff. | Zero added dependencies. | `BLOCKER` |
| **REQ-11-VERIF-081** | Zero modifications to frozen analytical engines | Phases 11.2–11.10 | Python source code in `backend/aivara/drift/` and `backend/aivara/assurance/` remains bit-for-bit unchanged. | Git diff check over backend modules. | Git diff report. | Zero lines modified in frozen backend modules. | `BLOCKER` |

---

### Layer 12: Adversarial Mutation & Tampering Resistance Requirements (REQ-11-VERIF-082 to REQ-11-VERIF-085)

| Requirement ID | Verification Objective | Source Phase | Expected Behavior / Invariant | Test Strategy | Evidence Required | Pass Criteria | Severity |
|---|---|---|---|---|---|---|---|
| **REQ-11-VERIF-082** | Cryptographic payload mutation detection | Phases 11.2–11.9 | Modifying any single sample, label, feature value, or metadata field alters the corresponding cryptographic digest. | Systematic mutation test across all schema fields. | Digest comparison matrix. | 100% of mutations produce distinct SHA-256 hashes ($H_{\text{mut}} \ne H_{\text{orig}}$). | `BLOCKER` |
| **REQ-11-VERIF-083** | Adversarial label-flipping detection | Phase 11.4, 11.8 | Synthetic flipping of 20% of target labels triggers significant class distribution TVD and categorical drift. | Ingest dataset with 20% flipped target labels. | `FeatureDriftProfile.target_label_shift`. | $\text{TVD} \ge 0.15$; categorical test p-value $\le 0.001$; label shift finding generated. | `CRITICAL` |
| **REQ-11-VERIF-084** | Adversarial timestamp reordering resilience | Phase 11.7 | Shuffling sample timestamps does not alter temporal window partitioning due to deterministic chronological pre-sorting. | Feed identical sample set in randomized arrival order. | Window partition boundaries. | Window contents and computed drift trajectory are identical across all arrival order permutations. | `CRITICAL` |
| **REQ-11-VERIF-085** | Adversarial contributor identifier collision evasion | Phase 11.8 | Submitting malicious contributor strings (homoglyphs, control characters, leading/trailing whitespace) resolves to correct canonical group. | Submit permutations (`"user\x001"`, `" user1 "`, `"USER1"`, `"ｕｓｅｒ１"`). | Canonical group mapping. | All variants map to identical canonical identifier and pseudonym. | `CRITICAL` |
