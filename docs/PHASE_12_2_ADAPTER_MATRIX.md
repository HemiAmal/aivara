# PHASE 12.2 — DOMAIN ADAPTER MAPPING MATRIX

**Milestone**: Phase 12.2 Implementation & Post-Audit Reconciliation  
**Subsystem**: Universal Risk Engine — Evidence Normalization Layer  
**Status**: RECONCILED & AUDIT-READY  

---

## 1. Domain Adapter Specification Matrix

| Domain Enum | Upstream Source Phase | Adapter Class | Input Contract / Entities | Output Universal Fields | Provenance Mapping | Ancestry Mapping | Source Hash & Confidence Handling | Failure / Error Behavior | Version |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| **`DATASET_INTEGRITY`** | Phase 5 | `DatasetIntegrityEvidenceAdapter` | `ImageQualityEvidence`, `OODDistanceEvidence`, `LabelAnomalyEvidence`, `DatasetFingerprintManifest` | `evidence_id`, `project_id`, `domain`, `evidence_type`, `evidence_layer`, `severity`, `confidence`, `primary_asset_id` (dataset), `data_json`, `canonical_hash` | `provenance_record_ids`, `provenance_hashes` | `sample_id`, `dataset_version_id`, `source_id` | Recompute SHA-256 over `data_json` or raw bytes; preserve detection confidence | Mismatched project $\to$ `ProjectMismatchError`; `NaN` $\to$ `InvalidEvidenceError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`CONTRIBUTOR_RISK`** | Phase 6 | `ContributorRiskEvidenceAdapter` | `ContributorRiskProfile`, `LeaveOneOutMetrics`, `GroupSpecializationMetrics` | Same as above with `primary_asset_id` (contributor), `source_id`, `empirical_bayes` scores | `provenance_record_ids`, `provenance_hashes` | `source_id` (contributor_id), `dataset_version_id` | Recompute SHA-256 over `data_json`; preserve detection confidence | Mismatched project $\to$ `ProjectMismatchError`; Missing ancestry $\to$ `UNVERIFIED`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`MODEL_INTEGRITY`** | Phase 7 | `ModelIntegrityEvidenceAdapter` | `ModelFingerprintModel`, `WeightHashMerkleProof`, `QuantizationDelta` | Same as above with `primary_asset_id` (model), `evidence_layer="proof"`, `confidence=1.0` | `provenance_record_ids`, `provenance_hashes` | `model_fingerprint` | Recompute SHA-256 over `data_json`; strictly enforce `confidence=1.0` | Proof confidence $\ne 1.0$ $\to$ `InvalidEvidenceError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`BEHAVIORAL_ANALYSIS`** | Phase 8 | `BehavioralAnalysisEvidenceAdapter` | `OutputStabilityMetric`, `PerturbationSensitivity`, `BehavioralDriftProfile` | Same as above with `primary_asset_id` (model), `stability_metrics` | `provenance_record_ids`, `provenance_hashes` | `model_fingerprint`, `window_id` (baseline_run_id) | Recompute SHA-256 over `data_json`; preserve detection confidence | Non-finite floats $\to$ `InvalidEvidenceError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`BACKDOOR_TRIGGER`** | Phase 9 | `BackdoorTriggerEvidenceAdapter` | `PatchTriggerActivation`, `SpectralSignatureAnomaly`, `TriggerCandidateResult` | Same as above with `primary_asset_id` (model), `trigger_payload` | `provenance_record_ids`, `provenance_hashes` | `model_fingerprint`, `dataset_version_id` | Recompute SHA-256 over `data_json`; preserve detection confidence | Mismatched project $\to$ `ProjectMismatchError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`INFERENCE_INTEGRITY`** | Phase 10 | `InferenceIntegrityEvidenceAdapter` | `18CheckpointVerificationResult`, `InputModelBindingDigest`, `ReplayEvidence` | Same as above with `primary_asset_id` (inference_id), `evidence_layer="proof"`, `confidence=1.0` | `provenance_record_ids`, `provenance_hashes` | `model_fingerprint`, `sample_id` (input_hash) | Recompute SHA-256 over `data_json`; strictly enforce `confidence=1.0` | Proof confidence $\ne 1.0$ $\to$ `InvalidEvidenceError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
| **`DISTRIBUTION_SHIFT`** | Phase 11 | `DistributionShiftEvidenceAdapter` | `FeatureDatasetDrift`, `ImageDistributionShift`, `RepresentationShift`, `TemporalDrift`, `SourceDrift` | Same as above with `primary_asset_id` (target_asset_id), `drift_metrics` | `provenance_record_ids`, `provenance_hashes` | `dataset_version_id`, `model_fingerprint`, `window_id`, `source_id` | Recompute SHA-256 over `data_json`; preserve detection confidence | Mismatched project $\to$ `ProjectMismatchError`; Hash mismatch $\to$ `SourceHashMismatchError` | `1.0.0` |
